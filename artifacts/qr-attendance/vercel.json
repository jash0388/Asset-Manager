{
  "buildCommand": "cd ../.. && pnpm install && cd artifacts/qr-attendance && pnpm run build",
  "outputDirectory": "dist/public",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
